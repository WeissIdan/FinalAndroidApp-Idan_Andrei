<?xml version="1.0" encoding="utf-8"?>
<!-- The 3 bottom-nav tabs. Each item's id intentionally matches the id of the
     destination it should open in nav_graph.xml - that's how
     NavigationUI.setupWithNavController() (in MainActivity) wires taps to navigation. -->
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/speedTestFragment"
        android:icon="@drawable/ic_speed"
        android:title="@string/title_speed_test" />

    <item
        android:id="@+id/historyFragment"
        android:icon="@drawable/ic_history"
        android:title="@string/title_history" />

    <item
        android:id="@+id/settingsFragment"
        android:icon="@drawable/ic_settings"
        android:title="@string/title_settings" />

</menu>
